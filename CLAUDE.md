# منهاجي — لوحة الإدارة

> يعمل على هذا المستودع **مطوّران** بمساعدَين منفصلَين. اقرأ هذا الملف أوّلًا.

## قبل أي شغل — إلزامي

```bash
git pull --rebase
git log --oneline -10
node test/smoke.js
```

## ما هذا المستودع

لوحة إدارة المحتوى والحسابات: المواد · الدروس · بنك الأسئلة · الأساتذة ·
البانرات · الطلاب · أكواد التفعيل · الطاقم · سجلّ النشاط.

**JavaScript صافٍ بلا أدوات بناء**، بنفس أسلوب تطبيق الطالب. التعليقات بالعربية
وتشرح **لماذا** لا ماذا.

## قواعد لا تُكسر

**١. الترقيم إلزامي.** PostgREST يقصّ عند ١٠٠٠ صفّ بلا رسالة خطأ. وقع هذا
فعلًا هنا: جدول الخيارات تجاوز الحدّ، فكان المحرّر يفتح سؤالًا موجودةً خياراته
في القاعدة ويراه فارغًا **فيضيفها من جديد** — تكرارٌ في البيانات سببه عرضٌ
ناقص. `Api.from()` تُرقّم تلقائيًا؛ لا تلتفّ حولها.

**٢. الصلاحية يفرضها السيرفر لا هذه اللوحة.**
إخفاء زرّ ليس حمايةً. القاعدة ترفض بـ`403` وهي المرجع. وأخفِ ما يرفضه
السيرفر أيضًا: صفحةٌ تُفتح ثم تفشل كل عملياتها تجربةٌ سيّئة لا حماية.

**٣. المنع في RLS يعود `200` بمصفوفة فارغة لا خطأً.**
اطلب `Prefer: return=representation` وافحص المصفوفة، وإلّا قرأت المنع نجاحًا
وأخبرت المحرّر أن تعديله حُفظ وهو لم يُحفظ.

**٤. اقرأ هويّة المستخدم من التوكن لا من أوّل صفّ.**
المدير يرى **كل** الملفّات الشخصية، فقراءة `profiles` بلا مرشِّح وأخذ أوّل صفّ
تعطيه هويّة شخص آخر ودوره. حدث فعلًا: أوّل مدير دخل ظهر باسم أستاذ.
استعمل `Api.userId()`.

**٥. المعاينة تطابق ما يراه الطالب.**
صورة الأستاذ غلافُ شاشة المادة، وهو **مربّع** (`height: min(100vw, …)` على
`.chero__band`)؛ فإطار القصّ هنا مربّعٌ مثله. معاينةٌ بنسبةٍ أخرى تجعل المحرّر
يضبط الوجه على إطارٍ لا وجود له فيوافق على قصٍّ لن يراه.

النسبة مكتوبة في **مستودعين**، وانحرافهما خطأٌ صامت لا يظهر إلّا في هاتف
طالب — فحارسٌ في `test/smoke.js` يقرأ `css/app.css` من مستودع التطبيق ويسقط
إن اختلفا (يُتخطّى بوضوح إن لم يُعثر عليه؛ اضبط `MANHAJI_APP`).

## نقطة مفتوحة

الصور تُرفع **كما هي حتى ٥ م.ب بلا تصغير**. صورة من كاميرا موبايل ينزّلها كل
طالب كاملةً وتُعرض بعرض ~٣٥٠ بكسل. الحلّ: تصغير على `canvas` قبل الرفع
(~١٦٠٠ بكسل، WebP). لم يُنفَّذ بعد.

## الفحص

```bash
node test/smoke.js
```

**أثبت الخلل بفحصٍ يسقط أوّلًا، ثم أصلح، ثم أثبت أنه صار يمرّ.** وإن مرّ فحصٌ
جديد من أوّل مرّة، اجعله يسقط عمدًا لتتأكّد أنه يفحص شيئًا.

## مستودعات أخرى

- `manhaji` — تطبيق الطالب
- `manhaji-backend` — القاعدة والهجرات والمسابير

## أسلوب الكوميت

بالعربية. **لماذا** لا ماذا.
